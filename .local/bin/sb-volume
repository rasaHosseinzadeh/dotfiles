#!/bin/sh



vol=$(pamixer --get-volume)
if [ "$vol" -gt "70" ]; then
	icon="墳"
elif [ "$vol" -lt "30" ]; then
	icon="奄"
else
	icon="奔"
fi

pamixer --get-mute | grep -q "true" && icon="ﱝ"

printf "\03 [%1s%3d%%]\n" $icon $vol
